version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - goconst
formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
run:
  timeout: 3m
